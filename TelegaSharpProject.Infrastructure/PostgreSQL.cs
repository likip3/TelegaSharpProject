using TelegaSharpProject.Infrastructure.Data;
using TelegaSharpProject.Infrastructure.Models;

namespace TelegaSharpProject.Infrastructure
{
    public class PostgreSQL
    {
        //public async Task Connect()
        //{
        //    // добавление данных
        //    using (TelegaSharpProjectContext db = new TelegaSharpProjectContext())
        //    {
        //        // создаем два объекта User
        //        User user1 = new User { UserName = "Tom", Id = 33 };
        //        User user2 = new User { UserName = "Alice", Id = 26 };

        //        // добавляем их в бд
        //        db.Users.AddRange(user1, user2);
        //        db.SaveChanges();
        //    }
        //    // получение данных
        //    using (TelegaSharpProjectContext db = new TelegaSharpProjectContext())
        //    {
        //        // получаем объекты из бд и выводим на консоль
        //        var users = db.Users.ToList();
        //        Console.WriteLine("Users list:");
        //        foreach (User u in users)
        //        {
        //            Console.WriteLine($"{u.Id}.{u.UserName} - {u.Id}");
        //        }
        //    }

        //}
    }
}
